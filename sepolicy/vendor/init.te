allow init hwservicemanager:binder { call transfer };
allow init ipa_dev:chr_file open;
allow init ion_device:chr_file ioctl;
allow init property_socket:sock_file write;
allow init sysfs_dm:file { open write };
allow init vendor_default_prop:property_service set;
allow init sysfs_info:file { open read };
allow init sysfs:file setattr;
allow init persist_block_device:lnk_file relabelto;
allow init sysfs_msm_perf:file { write };
allow init sysfs_msm_perf:file { open };
allow init platform_app:fd { use };
allow init qti_exec:file { execute_no_trans };
allow init qti_exec:file { execute };
allow init untrusted_app_27:fd { use };
allow init ion_device:chr_file { ioctl };
allow init init:socket { write };
allow init init:socket { create };
allow init init:socket { read };
allow init sysfs_devfreq:file { read };
allow init sysfs_kgsl:file { read };
allow init sysfs_graphics:file { write };
allow init sysfs_graphics:file { open };
allow init radio:binder { call };
allow init sensors_device:chr_file { ioctl };
allow init vendor_file:file { execute };
allow init diag_device:chr_file { read write};
allow init surfaceflinger:fd { use };
